// import { RESUME_THEME_REGISTRY } from "../../../(dashboard)/resumes/templates/templateRegistry";

// export default function ClassicLeftResume({
//   resume,
// }: {
//   resume: ResumeTemplateData;
// }) {
//   const theme = RESUME_THEME_REGISTRY.find(
//     (item) => item.id === "classic-left",
//   )!;

//   return <ResumeTemplateRenderer resume={resume} theme={theme} />;
// }
import React from "react";

const ClassicLeftResume = () => {
  return <div>ClassicLeftResume</div>;
};

export default ClassicLeftResume;
